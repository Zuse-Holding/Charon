# Dashboard (Vercel env) — public, browser-safe
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only — used by app/api routes. NEVER expose to the client bundle.
SUPABASE_SERVICE_ROLE_KEY=
